machine:
 python:
  version: 3.6.0

dependencies:
 post:
 - pip install requests
 - pip install pylint
 - pip install pytest
 - pip install pytest-mock
 - pip install mock

test:
 override:
 - pylint bin/chroma-feedback src tests
 - pytest tests
build: false

image:
- Visual Studio 2019

environment:
 matrix:
 - TOXENV: py39

test_script:
- pip install mock
- pip install pyqt5
- pip install pyqt5-stubs
- pip install pytest
- pip install pytest-mock
- pip install requests
- pytest tests

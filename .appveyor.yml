build: false

platform:
- x64

install:
- pip install request
- pip install pylint
- pip install pytest
- pip install pytest-mock
- pip install mock

test_script:
- pylint bin/chroma-feedback chroma_feedback tests
- pytest tests
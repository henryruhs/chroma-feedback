#!/usr/bin/env python3

import sys
import signal
from src.core import run, destroy

# run as needed

signal.signal(signal.SIGINT, destroy)
if len(sys.argv) == 2:
	run(sys.argv[1], 0)
elif len(sys.argv) == 3:
	run(sys.argv[1], int(sys.argv[2]))
else:
	exit(1)

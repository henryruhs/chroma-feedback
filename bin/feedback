#!/usr/bin/env python3

import sys
sys.path.append('./src')
from core import run

# run as needed

if len(sys.argv) == 2:
	run(sys.argv[1], 0)
elif len(sys.argv) == 3:
	run(sys.argv[1], int(sys.argv[2]))
else:
	sys.exit(1)
